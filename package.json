{
  "name": "hot-beans-web",
  "version": "1.0.0",
  "description": "Hot Beans web application",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run server\" \"npm run browser-sync\"",
    "server": "nodemon app.js",
    "browser-sync": "browser-sync start --proxy 'localhost:3000' --files 'public/**/*.*, views/**/*.*' --port 4000 --no-open --no-notify"
  },
  "keywords": [],
  "author": "Emmy",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2",
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  }
}
